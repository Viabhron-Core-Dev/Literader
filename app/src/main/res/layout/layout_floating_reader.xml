<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- FOLDED STATE (BUBBLE) -->
    <ImageView
        android:id="@+id/bubble_icon"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:src="@drawable/ic_launcher"
        android:elevation="8dp"
        android:visibility="visible" />

    <!-- EXPANDED STATE (WINDOW) -->
    <LinearLayout
        android:id="@+id/window_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="#222222"
        android:visibility="gone">

        <!-- Permanent Top Bar -->
        <LinearLayout
            android:id="@+id/top_drag_bar"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:background="#151515"
            android:orientation="horizontal"
            android:gravity="center_vertical">
            <TextView
                android:id="@+id/tv_window_title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:paddingStart="12dp"
                android:textColor="#AAAAAA"
                android:textSize="14sp"
                android:singleLine="true"
                android:text="LiteReader" />
            
            <ImageView
                android:id="@+id/btn_exit"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:padding="10dp"
                android:src="@android:drawable/ic_menu_close_clear_cancel"
                android:tint="#888888" />
        </LinearLayout>

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1">

            <!-- Main text content -->
            <ScrollView
                android:id="@+id/scroll_view"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:fillViewport="true">
                <TextView
                    android:id="@+id/tv_content"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:padding="16dp"
                    android:textColor="#DDDDDD"
                    android:textSize="18sp"
                    android:lineSpacingExtra="6dp"
                    android:text="Loading..."/>
            </ScrollView>

            <!-- Toggleable Toolbar (Moonreader style) -->
            <LinearLayout
                android:id="@+id/toolbar_container"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom"
                android:orientation="vertical"
                android:background="#DD111111"
                android:visibility="gone">
                
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:padding="12dp">
                    <TextView
                        android:id="@+id/tv_chapter_title"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textColor="#FFFFFF"
                        android:textSize="14sp"
                        android:textStyle="bold"
                        android:text="Chapter Title" />
                    <Space android:layout_width="0dp" android:layout_height="0dp" android:layout_weight="1"/>
                    <TextView
                        android:id="@+id/tv_progress"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textColor="#AAAAAA"
                        android:textSize="12sp"
                        android:text="0%" />
                </LinearLayout>

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center"
                    android:padding="8dp">

                    <ImageView android:id="@+id/btn_prev"
                        android:layout_width="0dp" android:layout_weight="1" android:layout_height="48dp" android:padding="12dp"
                        android:src="@android:drawable/ic_media_previous" android:tint="#FFFFFF" />

                    <!-- Search, library, settings on demand -->
                    <ImageView android:id="@+id/btn_search"
                        android:layout_width="0dp" android:layout_weight="1" android:layout_height="48dp" android:padding="12dp"
                        android:src="@android:drawable/ic_menu_search" android:tint="#FFFFFF" />

                    <ImageView android:id="@+id/btn_auto_scroll"
                        android:layout_width="0dp" android:layout_weight="1" android:layout_height="48dp" android:padding="12dp"
                        android:src="@android:drawable/ic_menu_sort_by_size" android:tint="#FFFFFF" />

                    <ImageView android:id="@+id/btn_library"
                        android:layout_width="0dp" android:layout_weight="1" android:layout_height="48dp" android:padding="12dp"
                        android:src="@android:drawable/ic_menu_gallery" android:tint="#FFFFFF" />

                    <ImageView android:id="@+id/btn_settings"
                        android:layout_width="0dp" android:layout_weight="1" android:layout_height="48dp" android:padding="12dp"
                        android:src="@android:drawable/ic_menu_preferences" android:tint="#FFFFFF" />

                    <ImageView android:id="@+id/btn_tts"
                        android:layout_width="0dp" android:layout_weight="1" android:layout_height="48dp" android:padding="12dp"
                        android:src="@android:drawable/ic_media_play" android:tint="#FFFFFF" />

                    <ImageView android:id="@+id/btn_next"
                        android:layout_width="0dp" android:layout_weight="1" android:layout_height="48dp" android:padding="12dp"
                        android:src="@android:drawable/ic_media_next" android:tint="#FFFFFF" />
                </LinearLayout>
            </LinearLayout>
        </FrameLayout>

        <!-- Bottom right resize handle -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="24dp"
            android:background="#151515" >
            <ImageView
                android:id="@+id/resize_handle"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_gravity="end"
                android:padding="4dp"
                android:src="@android:drawable/ic_menu_crop"
                android:tint="#555555" />
        </FrameLayout>
    </LinearLayout>
</FrameLayout>
